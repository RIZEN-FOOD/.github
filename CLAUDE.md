# 라이즌푸드 공식 웹사이트

## 프로젝트 정체
크림오브라이스 단일 제품 브랜드 사이트 + 관리자 시스템.
실제 운영될 상용 사이트이며 클라이언트가 있는 프로젝트다.

상세 기획은 docs/기획서.md 를 읽는다. 작업 전 반드시 참조할 것.
디자인 기준은 docs/prototype.html — 팔레트, 구조, 인터랙션의 레퍼런스다.

## 1차 범위
메인 / 상품(목록·상세) / 공지사항 / 인증 / 마이페이지 / 관리자
+ 자사몰 커머스: 장바구니 · 주문 · 결제 · 위시리스트 · 후기 · 취소반품교환

기획서 §13의 미확정 1·2번이 확정됐다 — AUTH 목적은 주문·결제이고 판매 방식은 자사몰이다.
2026-08-27 승인. 기획서 본문(범위 밖으로 적힌 부분)보다 이 문서가 우선한다.

이벤트·쿠폰은 테이블만 세워뒀다. 요건이 확정되지 않았으므로 화면과 API 를 붙이지 마라.

## 기술 스택
- 프론트: Next.js 15 (App Router) + TypeScript + Tailwind CSS
- 애니메이션: GSAP + ScrollTrigger + Lenis
- 백엔드: Spring Boot 3 + Java 21 (LTS) + Spring Data JPA
- DB: PostgreSQL
- 인증: Spring Security + JWT + OAuth2 (카카오·네이버)
- 이미지: S3 + CloudFront
- 결제: PG 미확정 (포트원 유력). payment 테이블은 PG 중립 구조이며 어댑터만 갈아끼운다
- 배포: Vercel(프론트) + EC2(API)

## 저장소 구조
    FE/             Next.js
    BE/             Spring Boot
    docs/           기획서, 프로토타입, ADR
    docker-compose.yml   PostgreSQL (로컬 개발)

## ★ 반드시 지킬 것

### 1. 식품 표시·광고 규제
크림오브라이스는 일반 식품이지 건강기능식품이 아니다.
효능·효과 표현은 식품표시광고법 위반이다.
  금지: "근육 증가에 도움", "다이어트 효과", "소화가 잘 됩니다", "위장에 좋은"
  허용: "운동 후 탄수화물 보충", "곱게 도정한 형태", "자극이 적은 담백한 맛"
카피를 새로 쓸 때마다 이 기준으로 검토하고, 애매하면 나에게 물어라.

### 2. 법정 표시사항은 텍스트로
영양성분·원재료·유통기한 등은 상세 이미지가 아니라 DOM 텍스트로 넣는다.
이미지 안의 글자는 검색에 안 잡히고 접근성에도 불리하다.

### 3. 처음부터 다제품 구조
지금은 제품이 하나지만 데이터 모델·화면 모두 여러 제품을 전제로 만든다.
상품을 하드코딩하지 마라. 전부 DB에서 온다.

### 4. 비개발자가 쓰는 관리자
대표가 개발자 없이 상품·배너·공지를 관리해야 한다.
  - 전문용어 금지 ("슬러그" → "주소에 쓸 영문 이름")
  - 이미지 업로드 칸에 권장 크기 안내 문구 필수
  - 저장 전 미리보기, 삭제는 이중 확인

### 5. 커머스 정합성 (타협 없음)
  - 금액을 클라이언트에서 받지 마라. 주문 금액은 서버가 상품 테이블을 다시 읽어 계산한다
  - PG 승인 금액과 서버 계산 금액을 대조한 뒤에만 주문을 확정한다
  - 재고 차감은 원자적으로: UPDATE ... WHERE stock >= qty. 읽고-나서-쓰기 금지
  - 주문번호에 순번을 쓰지 마라. 추측되면 남의 주문을 훑을 수 있다
  - 주문에는 배송지·상품명·가격을 스냅샷으로 박는다. 원본이 바뀌어도 기록은 그대로여야 한다
  - 배송비 임계액(현재 3,500원 / 50,000원 이상 무료 / 도서산간 +3,000원)은 shipping_policy 에서 읽는다

### 6. 보안 (타협 없음)
  - CMS 에디터 입력은 저장 시 HTML 살균 (허용 목록 방식)
  - 파일 업로드: 확장자·MIME·시그니처 검증 + 이미지 재인코딩 + S3 서빙
  - 모든 관리 API에 @PreAuthorize
  - JWT는 HttpOnly + Secure + SameSite 쿠키. localStorage 금지
  - 비밀번호 BCrypt, 로그인 5회 실패 시 10분 잠금
  - 휴대폰 번호 컬럼 암호화 (member, address, orders, inquiry 전부)
  - .env 는 절대 커밋하지 않는다. .env.example 만

### 7. 전자상거래법
  - 통신판매업 신고번호·사업자정보를 푸터에 게시한다 (site_setting 에서 읽는다)
  - 청약철회 안내와 배송·교환·환불 정책 페이지는 법정 의무다
  - 취소·반품·교환 요청은 order_claim 에 요청 시각과 처리 시각을 반드시 남긴다

## 작업 방식
1. 코드 변경 시 파일 전체 출력. 부분 스니펫 금지
2. 구현 전 "무엇을/왜/어떻게" 3줄 계획을 먼저 보여준다
3. DB 스키마 변경과 카피 문구는 내 승인을 받고 진행
4. 스코프 크리프 금지. 요청 범위만

## 하지 말 것
- 상품 정보·가격·배송비를 코드에 하드코딩 (전부 DB 에서 온다)
- 이벤트·쿠폰 화면 (요건 확정 전)
- 관리자 화면에 실제로 쓰지 않을 차트·지표 추가
- 효능·효과를 암시하는 카피
